## Summary

## Checklist
- [ ] `npm run lint`, `npx tsc --noEmit` and `npm run build` pass
- [ ] If this adds or changes an example: every sample was run against the real API and lands on the intended decision (results below)
- [ ] No API keys or personal data in the diff

## Results (for examples)
| Sample | Key answers | Decision |
| --- | --- | --- |
|  |  |  |
